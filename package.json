{
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=lagrangeapi",
    "dev:web": "npm run dev --workspace=lagrangeweb",
    "build:web": "npm run build --workspace=lagrangeweb",
    "build:api": "npm run build --workspace=lagrangeapi",
    "build": "npm run build:web"
  }
}